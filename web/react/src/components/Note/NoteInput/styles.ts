import { motion } from 'framer-motion';
import styled from 'styled-components';

export const InputTitle = styled(motion.input)`
  color: #252525;
  background: none;
  border: none;
  font-size: 32px;
`;
