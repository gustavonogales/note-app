import styled from 'styled-components';

export const Button = styled.button`
  background: rgba(0, 0, 0, 0.15);
  color: #fff;
  border-radius: 8px;
  border: none;
  box-shadow: none;
  padding: 6px;
  text-align: center;
  min-height: 40px;
  min-width: 40px;
  font-size: 16px;
  line-height: 0;
  position: relative;
`;
